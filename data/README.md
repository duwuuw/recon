# Data

Place your **ImageFolder** dataset root here as `raw/dataset/` (class names as subfolders), or keep it elsewhere and set `RAICOM_DATA_ROOT` to that absolute path.

Alternatively, a folder `weather_dataset/dataset/` at the repository root is detected automatically if present.

This directory is gitignored except `.gitkeep` so large image sets are not pushed by mistake.
