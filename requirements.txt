# Install PyTorch for your platform from https://pytorch.org/ first if needed, then:
# pip install -r requirements.txt
# Editable package (adds src to path for `from raicom.paths import ...`):
# pip install -e .

numpy>=1.24
torch>=2.0
torchvision>=0.15
timm>=0.9
tqdm>=4.65
matplotlib>=3.7
scikit-learn>=1.3
pandas>=2.0
xgboost>=2.0
